import pkg from "mongoose";

const ProductSchema = pkg.Schema({
    name: {
        type: String,
        required: true
    },
    price: {
        type: Number,
        required: true
    },
    image: {
        type: String,
        required: true
    },
});

export default pkg.model('Product', ProductSchema);